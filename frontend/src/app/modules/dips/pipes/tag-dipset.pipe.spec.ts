import { TagDipsetPipe } from './tag-dipset.pipe';

describe('TagDipsetPipe', () => {
  it('create an instance', () => {
    const pipe = new TagDipsetPipe();
    expect(pipe).toBeTruthy();
  });
});
